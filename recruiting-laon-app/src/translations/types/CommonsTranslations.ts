export type CommonsTranslations = {
  validation: {
    required: string;
    email: string;
    passwordWhiteSpace: string;
  };
  register: string;
  enter: string;
  changeLanguage: string;
  logout: string;
};
